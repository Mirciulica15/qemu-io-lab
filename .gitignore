# Tool caches
.ruff_cache/
__pycache__/
*.pyc
.DS_Store

# Build / run artifacts (regenerated; never commit)
dist/
*.qcow2
*.img
*.verifynvme       # scratch NVMe backing created by builder/verify.py
labcd.iso          # generated from check.sh by builder/make-labcd.sh

# Large, downloadable installer ISO (pinned in builder/build-image.sh)
alpine-virt-*.iso
